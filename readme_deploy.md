# Gmail Trader Bot - Інструкція для Railway

## 📁 Структура файлів (плоска, все в корені)

```
gmail-trader-bot/
├── main.py              # Точка входу
├── config.py            # Конфігурація
├── models.py            # Моделі БД
├── database.py          # Підключення до БД
├── api_client.py        # Gmail Farmer API
├── order_processor.py   # Обробка ордерів
├── keyboards.py         # Інлайн клавіатури
├── handlers.py          # Всі хендлери
├── scheduler.py         # Фонові задачі
├── requirements.txt     # Залежності
├── Procfile            # Для Railway
├── runtime.txt         # Версія Python
└── .env                # Змінні (НЕ завантажувати на GitHub!)
```

## 🚀 Покрокова інструкція для Railway

### Крок 1: Підготовка файлів

1. Створи нову папку `gmail-trader-bot`
2. Скопіюй ВСІ файли з артефактів в цю папку:
   - `main.py`
   - `config.py`
   - `models.py`
   - `database.py`
   - `api_client.py`
   - `order_processor.py`
   - `keyboards.py`
   - `handlers.py`
   - `scheduler.py`
   - `requirements.txt`
   - `Procfile`
   - `runtime.txt`

### Крок 2: Отримати Telegram токен

1. Відкрий [@BotFather](https://t.me/BotFather)
2. Відправ `/newbot`
3. Введи назву бота (наприклад: `Gmail Trader Bot`)
4. Введи username (наприклад: `my_gmail_trader_bot`)
5. Скопіюй токен (виглядає так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Крок 3: Дізнатись свій Telegram ID

1. Відкрий [@userinfobot](https://t.me/userinfobot)
2. Натисни `/start`
3. Скопіюй свій ID (число, наприклад: `123456789`)

### Крок 4: Створити GitHub репозиторій

1. Іди на [github.com](https://github.com)
2. Створи новий репозиторій (кнопка "New")
3. Назва: `gmail-trader-bot`
4. **НЕ** додавай README, .gitignore чи license
5. Натисни "Create repository"

### Крок 5: Завантажити файли на GitHub

**Варіант А: Через веб-інтерфейс GitHub**
1. В новому репозиторії натисни "uploading an existing file"
2. Перетягни всі файли (крім .env якщо створював)
3. Натисни "Commit changes"

**Варіант Б: Через командний рядок**
```bash
cd gmail-trader-bot
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/твій-username/gmail-trader-bot.git
git push -u origin main
```

### Крок 6: Розгортання на Railway

1. **Зареєструйся на Railway**
   - Іди на [railway.app](https://railway.app)
   - Натисни "Login" → "Login with GitHub"
   - Авторизуй Railway

2. **Створи новий проєкт**
   - Натисни "+ New Project"
   - Обери "Deploy from GitHub repo"
   - Обери свій репозиторій `gmail-trader-bot`

3. **Додай PostgreSQL**
   - В проєкті натисни "+ New"
   - Обери "Database" → "Add PostgreSQL"
   - Почекай поки створюється

4. **Налаштуй змінні оточення**
   - Клікни на свій сервіс (gmail-trader-bot)
   - Перейди на вкладку "Variables"
   - Додай ці змінні (кнопка "+ New Variable"):

   ```
   BOT_TOKEN=твій_токен_від_BotFather
   OWNER_ID=твій_telegram_id
   API_DOMAIN=https://trade.gmailfarmer.com
   API_KEY=6e6fb747-a1cc-45e1-8f19-63b3dfef6490
   PRICE_CHECK_INTERVAL_MINUTES=5
   PRICE_NOTIFICATION_INTERVAL_MINUTES=60
   ```

   **ВАЖЛИВО:** `DATABASE_URL` додається автоматично з PostgreSQL!

5. **Запустити**
   - Збережи змінні
   - Railway автоматично задеплоїть бота
   - Перевір логи (вкладка "Deployments" → клік на останній деплой → "View Logs")

### Крок 7: Перевірка

1. Відкрий свого бота в Telegram
2. Відправ `/start`
3. Має з'явитись головне меню з кнопками

## 🔍 Як перевірити що працює

### Логи Railway

Відкрий "Deployments" → останній деплой → "View Logs"

**Має бути:**
```
Starting bot...
Initializing database...
Database initialized
Scheduler started
Bot started. Owner ID: 123456789
INFO:aiogram.dispatcher:Start polling
```

**Якщо помилки:**
- `Invalid token` → перевір BOT_TOKEN
- `Database connection` → перевір чи доданий PostgreSQL
- `API Error` → перевір API_KEY

### Тест функцій

1. **Головне меню** - `/start`
2. **Ціни** - натисни "📊 Поточні ціни"
3. **Баланс** - натисни "💰 Баланс API"
4. **Створення ордера** - натисни "➕ Створити ордер"

## 🛠 Troubleshooting

### Бот не відповідає
- Перевір чи правильний `BOT_TOKEN` в змінних
- Перевір логи в Railway
- Перевір чи запущений deployment

### Помилка БД
- Переконайся що PostgreSQL доданий
- `DATABASE_URL` має бути автоматично
- Перевір формат: `postgresql+asyncpg://...`

### Помилка API
- Перевір `API_KEY` в змінних
- Перевір чи є баланс в дашборді

### Ордери не виконуються
- Перевір логи планувальника
- Переконайся що є баланс на API
- Перевір інтервали в змінних

## 📝 Команди для локального тестування

```bash
# Встановити залежності
pip install -r requirements.txt

# Створити .env файл
echo "BOT_TOKEN=твій_токен" > .env
echo "OWNER_ID=твій_id" >> .env
echo "API_DOMAIN=https://trade.gmailfarmer.com" >> .env
echo "API_KEY=6e6fb747-a1cc-45e1-8f19-63b3dfef6490" >> .env
echo "DATABASE_URL=postgresql+asyncpg://user:pass@localhost/dbname" >> .env

# Запустити
python main.py
```

## 💡 Корисні посилання

- Railway Dashboard: https://railway.app/dashboard
- BotFather: https://t.me/BotFather
- Gmail Farmer Dashboard: https://trade.gmailfarmer.com

## ⚠️ Важливо

- НЕ публікуй `.env` файл на GitHub
- Зберігай токен бота в секреті
- Регулярно перевіряй баланс API
- Логи можна дивитись в Railway